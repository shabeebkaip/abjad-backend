# Dependencies
node_modules/
.pnp
.pnp.js

# Production build
dist/
build/
*.tsbuildinfo

# Vercel
.vercel

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Uploaded files
uploads/
temp/
tmp/

# Testing
# NOTE (2026-09-20): *.test.ts/*.test.js used to be blanket-ignored here,
# which silently kept the ENTIRE test suite (all __tests__ dirs, 165 tests
# across 8 files) out of version control — nothing was ever reviewable.
# dist/ (compiled output, incl. any compiled *.test.js) is already ignored
# above, so this was pure redundant over-matching. Removed so source tests
# are tracked like any other source file.
coverage/
.nyc_output

# IDE and editors
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
.AppleDouble
.LSOverride
*.sublime-project
*.sublime-workspace

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn
.yarn-integrity
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz

# Redis dump
dump.rdb

# MongoDB data (if running locally)
data/
mongodb/

# Cache
.cache/
.parcel-cache/

# Serverless directories
.serverless/

# FuseBox cache
.fusebox/

# DynamoDB Local files
.dynamodb/

# TernJS port file
.tern-port

# Stores VSCode versions used for testing VSCode extensions
.vscode-test

# Temporary files
*.tmp
*.bak
*.backup

# Lock files (optional - uncomment if not committing lock files)
# pnpm-lock.yaml
# package-lock.json
# yarn.lock
